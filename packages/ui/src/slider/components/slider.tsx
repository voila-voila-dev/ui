import { SliderRoot } from "#/slider/components/slider-root.tsx";
import { SliderValue } from "#/slider/components/slider-value.tsx";

/**
 * The Slider parts as one namespace.
 */
export const Slider = {
	Root: SliderRoot,
	Value: SliderValue,
};
