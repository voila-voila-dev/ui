import { ProgressLabel } from "#/progress/components/progress-label.tsx";
import { ProgressRoot } from "#/progress/components/progress-root.tsx";
import { ProgressValue } from "#/progress/components/progress-value.tsx";

/**
 * The Progress parts as one namespace.
 */
export const Progress = {
	Root: ProgressRoot,
	Label: ProgressLabel,
	Value: ProgressValue,
};
