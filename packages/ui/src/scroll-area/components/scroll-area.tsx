import { ScrollAreaBar } from "#/scroll-area/components/scroll-area-bar.tsx";
import { ScrollAreaRoot } from "#/scroll-area/components/scroll-area-root.tsx";

/**
 * The ScrollArea parts as one namespace.
 */
export const ScrollArea = {
	Root: ScrollAreaRoot,
	Bar: ScrollAreaBar,
};
