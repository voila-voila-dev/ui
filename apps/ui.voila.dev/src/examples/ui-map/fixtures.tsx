export const FRAME = "h-80 w-full overflow-hidden rounded-lg border";

export const NO_WEBGL = (
	<p className="p-6 text-muted-foreground text-sm">
		This browser has no WebGL, so the map cannot render.
	</p>
);
