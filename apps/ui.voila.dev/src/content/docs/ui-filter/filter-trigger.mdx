---
title: "Filter.Trigger"
description: "The search-shaped button that opens the editor."
sidebar:
  order: 4
---

import * as Example from "@/examples/ui-filter/filter-trigger";

<Preview layout="stack">
	<Example.Trigger />
</Preview>

```tsx
<Filter.Trigger
	summary={search}
	activeCount={2}
	labels={defaultFilterLabels}
	onClick={() => setOpen(true)}
/>;
```
