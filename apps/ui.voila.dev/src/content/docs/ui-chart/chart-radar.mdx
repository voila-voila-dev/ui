---
title: "Chart.Radar"
description: "One series across several axes."
sidebar:
  order: 22
---

import Preview from "../../../components/preview.astro";
import * as Example from "../../../examples/ui-chart/chart";

<Preview layout="stack">
	<Example.Radar client:load />
</Preview>

```tsx
<Chart.PolarGrid inset={92} />
<Chart.PolarAngleAxis inset={92} />
<Chart.Radar dots inset={92} />
```

`inset` reserves the ring's outer margin for the axis labels, which are drawn
outside it.
