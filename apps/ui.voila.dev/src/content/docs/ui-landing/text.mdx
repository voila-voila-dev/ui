---
title: "Text"
description: "Body copy for marketing pages: lead, default, muted, small."
sidebar:
  order: 7
---

import Preview from "@/components/docs/preview";
import * as Example from "../../../examples/ui-landing/sections";

<Preview layout="stack">
	<Example.Text />
</Preview>

```tsx
<Text variant="lead">La plateforme qui met en relation…</Text>
```

`variant`, `size`, `align` and `weight` are independent, and each is exported as
an options array.
