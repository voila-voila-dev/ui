---
title: "TestimonialGrid"
description: "Quote cards with an avatar, a role, and a star rating."
sidebar:
  order: 18
---

import * as Example from "@/examples/ui-landing/testimonial-grid";

<Preview layout="stack">
	<Example.Default />
</Preview>

```tsx
<TestimonialGrid.Root>
	<TestimonialGrid.Item>
		<TestimonialGrid.QuoteIcon />
		<TestimonialGrid.Quote>Finding a designer used to…</TestimonialGrid.Quote>
		<TestimonialGrid.Footer>
			<TestimonialGrid.Avatar accent="primary">R</TestimonialGrid.Avatar>
			<TestimonialGrid.Author>
				<TestimonialGrid.AuthorName>Head of product</TestimonialGrid.AuthorName>
				<TestimonialGrid.AuthorRole>Early-stage startup</TestimonialGrid.AuthorRole>
			</TestimonialGrid.Author>
			<TestimonialGrid.Rating />
		</TestimonialGrid.Footer>
	</TestimonialGrid.Item>
</TestimonialGrid.Root>
```

`Avatar` takes an initial rather than an image on purpose: a testimonial without
a photo should not look like one that is missing its photo.

## API

### TestimonialGrid.Root

<PropTable of="TestimonialGrid.Root" />

### TestimonialGrid.Author

<PropTable of="TestimonialGrid.Author" />

### TestimonialGrid.AuthorName

<PropTable of="TestimonialGrid.AuthorName" />

### TestimonialGrid.AuthorRole

<PropTable of="TestimonialGrid.AuthorRole" />

### TestimonialGrid.Avatar

<PropTable of="TestimonialGrid.Avatar" />

### TestimonialGrid.Footer

<PropTable of="TestimonialGrid.Footer" />

### TestimonialGrid.Item

<PropTable of="TestimonialGrid.Item" />

### TestimonialGrid.Quote

<PropTable of="TestimonialGrid.Quote" />

### TestimonialGrid.QuoteIcon

<PropTable of="TestimonialGrid.QuoteIcon" />

The five undescribed props on this part are Phosphor's own, shared by every
icon in the kit and not annotatable from here — see [Icon](/ui-icon/icon),
which covers what they do.

### TestimonialGrid.Rating

<PropTable of="TestimonialGrid.Rating" />
