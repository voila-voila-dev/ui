---
title: "Progress"
description: "A determinate or indeterminate progress bar, with an optional label."
sidebar:
  order: 22
---

import Preview from "@/components/docs/preview";
import * as Example from "../../../examples/ui/feedback";

<Preview layout="stack">
	<Example.ProgressDefault />
</Preview>

```tsx
<Progress.Root value={35}>
	<Progress.Label>Profile completion</Progress.Label>
	<Progress.Value />
</Progress.Root>
```

`value={null}` is the indeterminate state — the bar loops instead of filling.
`Progress.Value` takes a formatter child when a percentage is not what you want
to show.
