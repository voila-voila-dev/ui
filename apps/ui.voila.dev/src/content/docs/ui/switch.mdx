---
title: "Switch"
description: "A setting that takes effect immediately."
sidebar:
  order: 15
---

import Preview from "../../../components/preview.astro";
import * as Example from "../../../examples/ui/forms";

<Preview layout="stack">
	<Example.SwitchExample client:load />
</Preview>

```tsx
<Switch id="notify-missions" defaultChecked />
<Label htmlFor="notify-missions">Notify me about new missions</Label>
```

A switch applies as soon as it is flipped; a [Checkbox](/ui/checkbox/) waits
for the form to be submitted. If there is a Save button underneath, it is a
checkbox.
