---
title: "Tooltip"
description: "A short label describing the control under the cursor."
sidebar:
  order: 39
---

import * as Example from "@/examples/ui/overlays";

<Preview layout="row">
	<Example.TooltipExample />
</Preview>

```tsx
<Tooltip.Root>
	<Tooltip.Trigger render={<Button size="icon" aria-label="Save" />}>
		<FloppyDiskIcon />
	</Tooltip.Trigger>
	<Tooltip.Content>Save report</Tooltip.Content>
</Tooltip.Root>
```

A tooltip is a label, not a place to put content — it cannot hold anything
focusable, and it never carries information that is not available elsewhere. An
icon-only button still needs its own `aria-label`.

## API

### Tooltip.Root

<PropTable of="Tooltip.Root" />

### Tooltip.Content

<PropTable of="Tooltip.Content" />

### Tooltip.Provider

<PropTable of="Tooltip.Provider" />

### Tooltip.Trigger

<PropTable of="Tooltip.Trigger" />
