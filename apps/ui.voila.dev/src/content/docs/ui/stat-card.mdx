---
title: "StatCard"
description: "One metric, its change, and an optional sparkline."
sidebar:
  order: 64
---

import * as Example from "@/examples/ui/display";

<Preview layout="stack">
	<Example.StatCardExample />
</Preview>

```tsx
<StatCard.Root>
	<StatCard.Header>
		<StatCard.Label>Projects published</StatCard.Label>
	</StatCard.Header>
	<StatCard.Value>38</StatCard.Value>
	<StatCard.Delta trend="up">+12% vs. last month</StatCard.Delta>
</StatCard.Root>
```

`trend` picks the arrow and the colour; it is separate from the text because a
falling number is not always bad — cancellations going down is a `down` trend
you would want to read as good.

`StatCard.Chart` is a full-bleed slot pinned to the bottom edge for a sparkline.
Constrain the chart's height on the `Chart.Root` itself.

## API

### StatCard.Root

<PropTable of="StatCard.Root" />

### StatCard.Chart

<PropTable of="StatCard.Chart" />

### StatCard.Delta

<PropTable of="StatCard.Delta" />

### StatCard.Header

<PropTable of="StatCard.Header" />

### StatCard.Label

<PropTable of="StatCard.Label" />

### StatCard.Value

<PropTable of="StatCard.Value" />
