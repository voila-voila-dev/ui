---
title: "Avatar"
description: "A person's picture, with initials as the fallback."
category: "data-display"
---

import * as Example from "@/examples/ui/avatar";

<Preview layout="row">
	<Example.AvatarExample />
</Preview>

```tsx
<Avatar.Root>
	<Avatar.Image src={user.avatarUrl} alt={user.name} />
	<Avatar.Fallback>CD</Avatar.Fallback>
</Avatar.Root>
```

The fallback shows while the image loads and stays if it fails, so always give
one. `Avatar.Group` overlaps a row of them and `Avatar.GroupCount` closes it with
an overflow count.

For a name and a role next to the picture, [UserAvatar](/ui/user-avatar) does
that in one component.

## API

### Avatar.Root

<PropTable of="Avatar.Root" />

### Avatar.Badge

<PropTable of="Avatar.Badge" />

### Avatar.Fallback

<PropTable of="Avatar.Fallback" />

### Avatar.Group

<PropTable of="Avatar.Group" />

### Avatar.GroupCount

<PropTable of="Avatar.GroupCount" />

### Avatar.Image

<PropTable of="Avatar.Image" />
