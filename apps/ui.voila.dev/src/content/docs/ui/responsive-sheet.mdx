---
title: "ResponsiveSheet"
description: "A side sheet on desktop, a bottom drawer on small screens."
category: "overlays"
---

import * as Example from "@/examples/ui/responsive-sheet";

<Preview layout="center">
	<Example.Default />
</Preview>

## Usage

```ts
import { ResponsiveSheet } from "@voila.dev/ui/responsive-sheet";
```

```tsx
<ResponsiveSheet.Root>
	<ResponsiveSheet.Trigger render={<Button variant="outline" />}>Open</ResponsiveSheet.Trigger>
	<ResponsiveSheet.Content size="xl">
		<ResponsiveSheet.Header>
			<ResponsiveSheet.Title>Email details</ResponsiveSheet.Title>
		</ResponsiveSheet.Header>
		<ResponsiveSheet.Body>…</ResponsiveSheet.Body>
	</ResponsiveSheet.Content>
</ResponsiveSheet.Root>
```

A [Sheet](/ui/sheet) on desktop and a bottom [Drawer](/ui/drawer) on small
screens. Reach for this over plain Sheet for detail panels, which are the ones
that hurt most on a phone: a side panel on a narrow viewport is either a sliver
or a modal pretending not to be one.

## Recipes

`size` sets the desktop panel width; the mobile drawer is always full-width,
because a partial bottom sheet on a phone wastes the only dimension it has.

As with [ResponsiveDialog](/ui/responsive-dialog), the root holds the open state
so crossing the breakpoint does not close the panel, and `autoFocus={false}`
keeps the keyboard down when the first field is a text input.

## API

### ResponsiveSheet.Root

<PropTable of="ResponsiveSheet.Root" />

### ResponsiveSheet.Body

<PropTable of="ResponsiveSheet.Body" />

### ResponsiveSheet.Close

<PropTable of="ResponsiveSheet.Close" />

### ResponsiveSheet.Content

<PropTable of="ResponsiveSheet.Content" />

### ResponsiveSheet.Description

<PropTable of="ResponsiveSheet.Description" />

### ResponsiveSheet.Footer

<PropTable of="ResponsiveSheet.Footer" />

### ResponsiveSheet.Header

<PropTable of="ResponsiveSheet.Header" />

### ResponsiveSheet.Title

<PropTable of="ResponsiveSheet.Title" />

### ResponsiveSheet.Trigger

<PropTable of="ResponsiveSheet.Trigger" />

## Related

[Sheet](/ui/sheet) · [ResponsiveDialog](/ui/responsive-dialog) ·
[Drawer](/ui/drawer)
