---
title: "MoneyInput"
description: "An amount with its currency."
category: "forms"
---

import * as Example from "@/examples/ui/money-input";

<Preview layout="stack">
	<Example.Default />
</Preview>

```tsx
<MoneyInput
	value={amount}
	onValueChange={setAmount}
	currency="EUR"
	currencyLabel="Currency"
/>
```

With no `onCurrencyChange` the trailing select is locked, for the common case
of an app that settles in one currency. The amount field stays fully legible
either way — a disabled currency must not grey out the number next to it.

## API

<PropTable of="MoneyInput" />
