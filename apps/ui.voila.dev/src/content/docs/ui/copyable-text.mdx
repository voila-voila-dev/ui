---
title: "CopyableText"
description: "A value with a copy button that confirms it copied."
sidebar:
  order: 73
---

import Preview from "@/components/docs/preview";
import * as Example from "../../../examples/ui/display";

<Preview layout="stack">
	<Example.CopyableTextExample />
</Preview>

```tsx
<CopyableText value="camille@example.com" />
<CopyableText value={mission.id} label="019f7557" />
```

`label` shows a short form while copying the whole `value` — the usual case for
an id you want visible but not in full.
