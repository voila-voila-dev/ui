---
title: "Empty"
description: "The empty state of a list, table or panel."
sidebar:
  order: 25
---

import Preview from "@/components/docs/preview";
import * as Example from "../../../examples/ui/feedback";

<Preview layout="stack">
	<Example.EmptyDefault />
</Preview>

```tsx
<Empty.Root bordered>
	<Empty.Header>
		<Empty.Media variant="icon"><CalendarPlusIcon /></Empty.Media>
		<Empty.Title>No projects yet</Empty.Title>
		<Empty.Description>Create your first project to…</Empty.Description>
	</Empty.Header>
	<Empty.Content>
		<Button>Create a project</Button>
	</Empty.Content>
</Empty.Root>
```

`Empty.Media` takes `variant="icon"` for a tinted icon tile, or plain children
for anything else — an avatar, an illustration.
