---
title: "ColorPicker"
description: "Pick one of the catalogue colours."
category: "forms"
---

import * as Example from "@/examples/ui/color-picker";

<Preview layout="center">
	<Example.ColorPickerExample />
</Preview>

```tsx
<ColorPicker value={color} onValueChange={setColor} />
```

The palette is the closed [Badge](/ui/badge) colour set, so a colour picked
here is a name you can store and render as a chip anywhere. It is not a
free-form colour wheel, on purpose.

## API

<PropTable of="ColorPicker" />
