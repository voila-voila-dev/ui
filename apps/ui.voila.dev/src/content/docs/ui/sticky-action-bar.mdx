---
title: "StickyActionBar"
description: "The action bar pinned to the bottom of the viewport on mobile."
category: "actions"
---

import * as Example from "@/examples/ui/sticky-action-bar";

<Preview layout="stack">
	<Example.StickyActionBarExample />
</Preview>

```tsx
<StickyActionBar>
	<Button variant="outline">Contacter</Button>
	<Button>Postuler</Button>
</StickyActionBar>
```

It hides from the `md` breakpoint up, on the assumption that the same actions
are already visible in the desktop layout. Pass `hideOnDesktop={false}` when
they are not — which is what the preview above does.

## API

<PropTable of="StickyActionBar" />
