---
title: "Card"
description: "A bordered surface grouping a title, some content and its actions."
sidebar:
  order: 60
---

import Preview from "@/components/docs/preview";
import * as Example from "../../../examples/ui/display";

<Preview layout="stack">
	<Example.CardExample />
</Preview>

```tsx
<Card>
	<CardHeader>
		<CardTitle>Brand designer — Launch week</CardTitle>
		<CardDescription>Northwind Studio · Saturday, June 20</CardDescription>
		<CardAction><Badge>Open</Badge></CardAction>
	</CardHeader>
	<CardContent>…</CardContent>
	<CardFooter className="justify-end gap-2">
		<Button size="sm">Apply</Button>
	</CardFooter>
</Card>
```

`CardAction` goes inside the header, not after it — the header is a grid and the
action occupies its right-hand column.

An `<img>` as the card's first child is detected and bled to the edges, so a
cover image needs no wrapper.
