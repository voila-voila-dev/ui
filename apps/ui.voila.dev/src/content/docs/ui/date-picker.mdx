---
title: "DatePicker"
description: "A date field opening a calendar."
category: "dates-and-time"
---

import * as Example from "@/examples/ui/date-picker";

<Preview layout="center">
	<Example.DatePickerExample />
</Preview>

```tsx
<DatePicker.Root
	value={date}
	onValueChange={setDate}
	placeholder="Project date"
	locale="en-GB"
/>
```

`calendarProps` passes through to the [Calendar](/ui/calendar) underneath —
`defaultMonth`, `disabled`, and so on. `DatePicker.Range` is the two-date
version, and `DatePicker.Trigger` is the field on its own.

## API

### DatePicker.Root

<PropTable of="DatePicker.Root" />

### DatePicker.Range

<PropTable of="DatePicker.Range" />

### DatePicker.Trigger

<PropTable of="DatePicker.Trigger" />
