---
title: "Textarea"
description: "A multi-line text field that grows with its content."
sidebar:
  order: 4
---

import Preview from "../../../components/preview.astro";
import * as Example from "../../../examples/ui/forms";

<Preview layout="stack">
	<Example.TextareaExample client:load />
</Preview>

```tsx
<Textarea placeholder="Describe the mission for the provider…" />
```

It auto-grows to `max-h-64` and then scrolls, so a long description never
pushes the submit button off the screen.
