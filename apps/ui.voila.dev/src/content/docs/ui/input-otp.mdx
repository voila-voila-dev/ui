---
title: "InputOTP"
description: "A one-time-code field, one box per digit."
sidebar:
  order: 8
---

import Preview from "@/components/docs/preview";
import * as Example from "../../../examples/ui/forms";

<Preview layout="center">
	<Example.InputOTPExample />
</Preview>

```tsx
<InputOTP.Root maxLength={6} pattern={REGEXP_ONLY_DIGITS}>
	<InputOTP.Group>
		<InputOTP.Slot index={0} />
		<InputOTP.Slot index={1} />
		<InputOTP.Slot index={2} />
	</InputOTP.Group>
	<InputOTP.Separator />
	<InputOTP.Group>…</InputOTP.Group>
</InputOTP.Root>
```

Built on [input-otp](https://input-otp.rodz.dev/), so it is one real input
behind the boxes — paste, autofill from an SMS, and select-all all work.
