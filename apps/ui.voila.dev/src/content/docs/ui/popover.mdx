---
title: "Popover"
description: "A small floating panel anchored to its trigger."
sidebar:
  order: 37
---

import Preview from "../../../components/preview.astro";
import * as Example from "../../../examples/ui/overlays";

<Preview>
	<Example.PopoverExample client:load />
</Preview>

```tsx
<Popover>
	<PopoverTrigger render={<Button variant="outline">Details</Button>} />
	<PopoverContent>
		<PopoverHeader>
			<PopoverTitle>Saturday match coverage</PopoverTitle>
			<PopoverDescription>Stade Rennais — June 14.</PopoverDescription>
		</PopoverHeader>
		<p className="text-muted-foreground">A physiotherapist is needed…</p>
	</PopoverContent>
</Popover>
```

A popover can hold focusable content; a [Tooltip](/ui/tooltip/) cannot. If
there is a button or an input inside, this is the one you want.
