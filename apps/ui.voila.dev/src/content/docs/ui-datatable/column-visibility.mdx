---
title: "Column visibility"
description: "A menu of every hideable column, plus CSV export."
sidebar:
  order: 6
---

import * as Example from "@/examples/ui-datatable/column-visibility";

<Preview layout="stack">
	<Example.ViewOptions />
</Preview>

```tsx
<DataTable.Root
	columns={columns}
	data={projects}
	toolbar={(table) => (
		<DataTable.Toolbar>
			<DataTable.Actions>
				<DataTable.ViewOptions table={table} />
				<DataTable.Export table={table} filename="projects.csv" />
			</DataTable.Actions>
		</DataTable.Toolbar>
	)}
/>;
```

`toolbar` takes a render prop when a control needs the table instance — the
column menu and the exporter both do.
