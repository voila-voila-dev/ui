---
title: "Column resizing"
description: "Drag a column edge, or nudge it with the arrow keys."
sidebar:
  order: 4
---

import * as Example from "@/examples/ui-datatable/column-resizing";

<Preview layout="stack">
	<Example.Resizing />
</Preview>

```tsx
<DataTable.Root columns={columns} data={projects} enableColumnResizing />;
```

The handle is a real button, so the width is reachable without a pointer:
focus it and press left/right. Pass `columnSizing` and `onColumnSizingChange`
to persist widths across mounts.
