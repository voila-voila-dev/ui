---
title: "Quick start"
description: "The email template editor that lives in your app, not someone else's SaaS."
sidebar:
  order: 0
---

import * as Example from "@/examples/ui-email-block-editor/quick-start";

The email template editor that lives in your app, not someone else's SaaS.

Marketing emails are usually the one part of a product designed somewhere
else, behind someone else's login, in someone else's brand tooling. This
package puts the whole editing surface inside your own app: a grey canvas, a
600px card, and fifteen blocks the author drags into place. The blocks, the
theme and every string are configuration you pass in, and the document it
produces is plain data your server renders into the sent email.

<Preview layout="stack" scroll>
	<Example.Editor />
</Preview>

This is the real component. Drag a block by its handle, edit the copy in
place, or select a block and open its settings.

## Install

```sh
bun add @voila.dev/ui @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

```css title="globals.css"
@import "@voila.dev/ui/styles/sources.css";
```

Peers: `react@19`, `react-dom@19`, `tailwindcss@4`.

## The 3-minute win

The editor is controlled: you hold the document, it dispatches changes. That
makes persistence one `JSON.stringify` away, and undo, autosave or drafts a
matter of what you do with the state.

```tsx
import {
	createEmailBlocks,
	EmailBlockEditor,
	type EmailEditorDocument,
	emptyEmailEditorDocument,
} from "@voila.dev/ui/email-block-editor";
import { useState } from "react";

// Built once, outside the component: this is the editor's configuration, not
// its state.
const BLOCKS = createEmailBlocks({ currency: "EUR" });

export function CampaignEditor() {
	const [document, setDocument] = useState<EmailEditorDocument>(
		emptyEmailEditorDocument,
	);

	return (
		<EmailBlockEditor
			blocks={BLOCKS}
			document={document}
			onDocumentChange={setDocument}
			onUploadImage={async (file) => {
				// Upload to your own storage, resolve with the public URL.
				const { url } = await uploadImage(file);
				return url;
			}}
		/>
	);
}
```

Omit `onUploadImage` to disable image uploads. Pass `headerSlot` and
`footerSlot` to replace the neutral header and footer placeholders with your
own chrome, `documentSettings` for fields that belong to the document rather
than to a block (a subject line, a preheader), and `generateBlockId` for
deterministic ids in tests.

## Arranging it differently

`EmailBlockEditor` is the parts put together the usual way. When you want a
different arrangement, compose them yourself — every part renders a sensible
default with no children, so this is the same editor:

```tsx
<EmailEditor.Root blocks={BLOCKS} document={document} onDocumentChange={setDocument}>
	<EmailEditor.Layout>
		<EmailEditor.Toolbar />
		<EmailEditor.Canvas />
		<EmailEditor.Sidebar />
	</EmailEditor.Layout>
	<EmailEditor.SettingsSheet />
</EmailEditor.Root>
```

Selection and preview are controlled if you pass them (`selectedBlockId` /
`onSelectedBlockIdChange`, `preview` / `onPreviewChange`) and internal if you
do not.

## Mental model

The document is plain data; the editor is one view of it. A document is
`{ version, blocks }`, every block a small readonly object with a `type`
field, and the whole thing serialises to JSON without ceremony. The editor
never emits HTML: placeholders like `{{firstName}}` stay as tokens, prices are
integers in minor units, and rich text is a flat list of spans. Your server
walks the same structure to produce the sent email, which is why the canvas
can promise to be an honest preview rather than an approximation.

Every building part is exported individually — the block definitions, the
option rows, the reducer, the hooks — so the composed `EmailBlockEditor` is a
convenience rather than a boundary.

## API

<PropTable of="EmailBlockEditor" />

Everything on the pages below is a *block* — a plain readonly object with a
`type` field, not a component with props — so those pages document a data
shape instead of a table. The definitions behind them are public too, so a
host can render the same blocks outside the editor.

## Page map

The blocks:

- [HeadingBlock](/ui-email-block-editor/heading-block): the email's title, or a section heading inside it.
- [ParagraphBlock](/ui-email-block-editor/paragraph-block): rich text as a flat list of spans.
- [ButtonBlock](/ui-email-block-editor/button-block): the call to action, filled or outlined.
- [ImageBlock](/ui-email-block-editor/image-block): an image, optionally linked, with a play-badge overlay for video thumbnails.
- [ListBlock](/ui-email-block-editor/list-block): bullets, numbers or badges, with an optional bold lead-in per item.
- [DividerBlock](/ui-email-block-editor/divider-block): the horizontal rule between sections.
- [GridBlock](/ui-email-block-editor/grid-block): a multi-column row any leaf block can sit in.
- [StatBlock](/ui-email-block-editor/stat-block): one figure with a label; three of them make a stats row in a grid.
- [TableBlock](/ui-email-block-editor/table-block): plain-text rows and columns, with an optional header row.
- [ArticleBlock](/ui-email-block-editor/article-block): a blog post or resource card.
- [ProductBlock](/ui-email-block-editor/product-block): a product card with price and compare-at price.
- [OfferBlock](/ui-email-block-editor/offer-block): a plan card with features and an optional highlight.
- [RatingBlock](/ui-email-block-editor/rating-block): a one-to-five question where each step is its own tracked link.
- [HighlightBlock](/ui-email-block-editor/highlight-block): one bold line on a brand-tinted panel.
- [FinePrintBlock](/ui-email-block-editor/fine-print-block): the small print at the foot of the email.

The features:

- [Server-side rendering](/ui-email-block-editor/server-side-rendering): walk the document to produce the sent email.
- [Custom blocks](/ui-email-block-editor/custom-blocks): add your own block type in one file.
- [Theming the canvas](/ui-email-block-editor/theming-the-canvas): the editor chrome follows your tokens; the email card follows your email.
