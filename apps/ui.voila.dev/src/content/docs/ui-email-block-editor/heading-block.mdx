---
title: "HeadingBlock"
description: "The email's title, or a section heading inside it."
sidebar:
  order: 1
---

import * as Example from "@/examples/ui-email-block-editor/heading-block";

<Preview layout="stack">
	<Example.Heading />
</Preview>

```ts
{ id: "heading", type: "heading", text: "Bonjour {{firstName}} !", level: 1 }
```

Placeholders like `{{firstName}}` are substituted when the email is rendered,
not in the editor, so the author sees the token rather than a sample value.
